/**
 * Balance colors matching Fava/Fava-dashboards (--green, --red from Fava's app.css).
 * Uses CSS variables when embedded in Fava so the host theme applies.
 */
export const BALANCE_COLOR_POSITIVE = "var(--green, hsl(151deg 100% 25%))";
export const BALANCE_COLOR_NEGATIVE = "var(--red, hsl(0deg 100% 30%))";
